defmodule AletopeltaTest.Day20241202 do
  use ExUnit.Case

  test "part1 is loaded" do
    assert Aletopelta.Day20241202.Part1.execute() == 1
  end

  test "part2 is loaded" do
    assert Aletopelta.Day20241202.Part2.execute() == 2
  end
end
